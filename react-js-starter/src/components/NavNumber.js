const Number = ({ value }) => {
  return (
    <span className="rounded-full bg-white w-8 h-8 flex items-center justify-center cursor-pointer">
      {value}
    </span>
  );
};

export default Number;
